/**
 * HairlineFrame — the ui.sh visual move: two thin horizontal rules at the top
 * and bottom of the viewport, creating a "window within a window" frame.
 *
 * This is applied via CSS on the body (.viewport-frame class) so it persists
 * across scroll and doesn't reflow. This component is a no-op placeholder for
 * documentation — the actual frame lives in globals.css under .viewport-frame.
 *
 * Left here as a named concept so future developers can find it by searching
 * "hairline frame" rather than hunting through CSS.
 */
export function HairlineFrame() {
  return null;
}
